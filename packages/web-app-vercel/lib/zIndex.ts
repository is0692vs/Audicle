// z-index values for consistent layering across the application
export const zIndex = {
    header: 10,
    mobileControls: 20,
    desktopControls: 40,
    modalBackdrop: 45,
    modal: 50,
    tooltip: 60,
} as const;